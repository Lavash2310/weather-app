name: CD

on:
  workflow_run:
    workflows: [CI]
    types:
      - completed

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install dependencies
        run: npm ci

  Deploy_Backend_Render:
    needs: Test
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Backend to Render
        uses: johnbeynon/render-deploy-action@v0.0.8
        with:
          service-id: ${{ secrets.RENDER_BACKEND_SERVICE_ID }}
          api-key: ${{ secrets.RENDER_API_KEY }}

  Deploy_Frontend_Render:
    needs: Deploy_Backend_Render
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Frontend to Render
        uses: johnbeynon/render-deploy-action@v0.0.8
        with:
          service-id: ${{ secrets.RENDER_FRONTEND_SERVICE_ID }}
          api-key: ${{ secrets.RENDER_API_KEY }}

  Build_Backend:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: self-hosted
    outputs:
      backend_image: ${{ steps.set-backend-image.outputs.image }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: jouk2310
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push backend image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.backend
          push: true
          tags: |
            jouk2310/weather-backend:latest
            jouk2310/weather-backend:${{ github.sha }}

      - name: Set backend image output
        id: set-backend-image
        run: echo "image=jouk2310/weather-backend:${{ github.sha }}" >> $GITHUB_OUTPUT

  Build_Frontend:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: self-hosted
    outputs:
      frontend_image: ${{ steps.set-frontend-image.outputs.image }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: jouk2310
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push frontend image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.frontend
          push: true
          tags: |
            jouk2310/weather-frontend:latest
            jouk2310/weather-frontend:${{ github.sha }}
          build-args: |
            VITE_BACKEND_URL=https://weather-backend-dde1.onrender.com

      - name: Set frontend image output
        id: set-frontend-image
        run: echo "image=jouk2310/weather-frontend:${{ github.sha }}" >> $GITHUB_OUTPUT

  Kubernetes_Deploy:
    needs: [Build_Backend, Build_Frontend]
    runs-on: self-hosted
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Kubeconfig
        run: |
          if [ -f "$HOME/.kube/config" ]; then
            echo "KUBECONFIG found at $HOME/.kube/config"
            echo "KUBECONFIG=$HOME/.kube/config" >> $GITHUB_ENV
          else
            echo "::error::Kubeconfig not found! Make sure Minikube is running."
            exit 1
          fi

      - name: Create Kubernetes Secret
        run: |
          kubectl delete secret my-app-secret --ignore-not-found
          kubectl create secret generic my-app-secret \
            --from-literal=api-key="${{ secrets.OPENWEATHER_API_KEY }}" \
            --from-literal=backend-url="https://weather-backend-dde1.onrender.com"

      - name: Apply Kubernetes manifests
        run: |
          kubectl apply -f kubernetes/ || echo "Some K8s manifests may not exist"

      - name: Update deployment images
        run: |
          BACKEND_IMAGE=${{ needs.Build_Backend.outputs.backend_image }}
          FRONTEND_IMAGE=${{ needs.Build_Frontend.outputs.frontend_image }}

          echo "Updating backend to $BACKEND_IMAGE"
          kubectl set image deployment/weather-backend backend=$BACKEND_IMAGE -n default || echo "⚠ Backend deployment not found"

          echo "Updating frontend to $FRONTEND_IMAGE" 
          kubectl set image deployment/weather-frontend frontend=$FRONTEND_IMAGE -n default || echo "⚠ Frontend deployment not found"

      - name: Wait for rollout
        run: |
          kubectl rollout status deployment/weather-backend -n default --timeout=300s || echo "⚠ Backend rollout timeout"
          kubectl rollout status deployment/weather-frontend -n default --timeout=300s || echo "⚠ Frontend rollout timeout"

      - name: Show resources
        run: kubectl get pods,svc,ingress -n default -o wide || echo "⚠ Failed to get K8s resources"

  Render_Email_Notification:
    needs: [Deploy_Backend_Render, Deploy_Frontend_Render]
    runs-on: ubuntu-latest
    if: ${{ always() }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install nodemailer
        run: npm install nodemailer --no-audit --no-fund

      - name: Send Render Deployment Email
        env:
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          MAIL_TO: ${{ secrets.MAIL_TO }}
          BACKEND_STATUS: ${{ needs.Deploy_Backend_Render.result }}
          FRONTEND_STATUS: ${{ needs.Deploy_Frontend_Render.result }}
          GIT_SHA: ${{ github.sha }}
          RUN_ID: ${{ github.run_id }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_ACTOR: ${{ github.actor }}
        run: |
          cat > send-render-email.cjs <<'EOF'
          const nodemailer = require("nodemailer");

          (async () => {
            try {
              const transporter = nodemailer.createTransport({
                service: 'gmail',
                auth: {
                  user: process.env.SMTP_USERNAME,
                  pass: process.env.SMTP_PASSWORD
                }
              });

              const backendSuccess = process.env.BACKEND_STATUS === 'success';
              const frontendSuccess = process.env.FRONTEND_STATUS === 'success';
              const overallSuccess = backendSuccess && frontendSuccess;
              
              const statusEmoji = overallSuccess ? '🚀' : '❌';
              const statusText = overallSuccess ? 'Render Deployment Successful' : 'Render Deployment Failed';
              const statusColor = overallSuccess ? '#1f883d' : '#d1242f';

              const htmlBody = `
                <!DOCTYPE html>
                <html>
                <head>
                    <style>
                        body { font-family: Arial, sans-serif; margin: 0; padding: 20px; background: #f6f8fa; }
                        .container { max-width: 600px; margin: 0 auto; background: white; border-radius: 6px; overflow: hidden; }
                        .header { background: ${statusColor}; color: white; padding: 20px; text-align: center; }
                        .content { padding: 20px; }
                        .status { padding: 10px; border-radius: 4px; margin: 10px 0; }
                        .success { background: #d1f5d3; color: #1f883d; }
                        .failure { background: #ffebe9; color: #d1242f; }
                        .info { background: #f6f8fa; padding: 15px; border-radius: 4px; margin: 15px 0; }
                    </style>
                </head>
                <body>
                    <div class="container">
                        <div class="header">
                            <h1>${statusEmoji} Weather App - Render Deployment</h1>
                        </div>
                        <div class="content">
                            <h2>${statusText}</h2>
                            
                            <div class="status ${backendSuccess ? 'success' : 'failure'}">
                                Backend: ${backendSuccess ? '✅ Success' : '❌ Failed'}
                            </div>
                            
                            <div class="status ${frontendSuccess ? 'success' : 'failure'}">
                                Frontend: ${frontendSuccess ? '✅ Success' : '❌ Failed'}
                            </div>
                            
                            <div class="info">
                                <strong>Repository:</strong> ${process.env.GITHUB_REPOSITORY}<br>
                                <strong>Author:</strong> ${process.env.GITHUB_ACTOR}<br>
                                <strong>Commit:</strong> ${process.env.GIT_SHA}<br>
                                <strong>Time:</strong> ${new Date().toISOString()}<br>
                                <strong>Backend URL:</strong> <a href="https://weather-backend-dde1.onrender.com">https://weather-backend-dde1.onrender.com</a><br>
                                <strong>Frontend URL:</strong> <a href="https://weather-frontend-7mo5.onrender.com">https://weather-frontend-7mo5.onrender.com</a>
                            </div>
                        </div>
                    </div>
                </body>
                </html>
              `;

              const mailOptions = {
                from: `Weather App CI/CD <${process.env.SMTP_USERNAME}>`,
                to: process.env.MAIL_TO,
                subject: `${statusEmoji} Weather App - Render ${statusText}`,
                html: htmlBody
              };

              await transporter.sendMail(mailOptions);
              console.log("✅ Render notification email sent successfully");

            } catch (error) {
              console.error("❌ Error sending Render email:", error);
              process.exit(1);
            }
          })();
          EOF

          node send-render-email.cjs

  Docker_Email_Notification:
    needs: [Build_Backend, Build_Frontend]
    runs-on: ubuntu-latest
    if: ${{ always() }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install nodemailer
        run: npm install nodemailer --no-audit --no-fund

      - name: Send Docker Build Email
        env:
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          MAIL_TO: ${{ secrets.MAIL_TO }}
          DOCKER_IMAGE_BACKEND: ${{ needs.Build_Backend.outputs.backend_image }}
          DOCKER_IMAGE_FRONTEND: ${{ needs.Build_Frontend.outputs.frontend_image }}
          BUILD_STATUS: ${{ needs.Build_Backend.result }}
          GIT_SHA: ${{ github.sha }}
          RUN_ID: ${{ github.run_id }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_ACTOR: ${{ github.actor }}
        run: |
          if [ -z "$SMTP_PASSWORD" ]; then
            echo "::error::SMTP_PASSWORD secret is empty. Add Gmail App Password to repo secrets."
            exit 1
          fi

          cat > send-docker-email.cjs <<'CJSEOF'
          const nodemailer = require("nodemailer");

          (async () => {
            try {
              const SMTP_USERNAME = process.env.SMTP_USERNAME;
              const SMTP_PASSWORD = process.env.SMTP_PASSWORD;
              const MAIL_TO = process.env.MAIL_TO;
              const DOCKER_IMAGE_BACKEND = process.env.DOCKER_IMAGE_BACKEND || '';
              const DOCKER_IMAGE_FRONTEND = process.env.DOCKER_IMAGE_FRONTEND || '';
              const BUILD_STATUS = process.env.BUILD_STATUS;
              const GIT_SHA = process.env.GIT_SHA;
              const RUN_ID = process.env.RUN_ID;
              const GITHUB_REPOSITORY = process.env.GITHUB_REPOSITORY;
              const GITHUB_ACTOR = process.env.GITHUB_ACTOR;

              console.log("Email From:", SMTP_USERNAME);
              console.log("Email To:", MAIL_TO);
              console.log("Build Status:", BUILD_STATUS);
              console.log("Backend Image:", DOCKER_IMAGE_BACKEND);
              console.log("Frontend Image:", DOCKER_IMAGE_FRONTEND);

              const transporter = nodemailer.createTransport({
                service: 'gmail',
                auth: {
                  user: SMTP_USERNAME,
                  pass: SMTP_PASSWORD
                }
              });

              await transporter.verify();
              console.log("✔ SMTP connection verified");

              const isSuccess = BUILD_STATUS === 'success';
              const statusText = isSuccess ? 'Build Successful' : 'Build Failed';
              const statusColor = isSuccess ? '#1f883d' : '#d1242f';
              const statusBg = isSuccess ? '#d1f5d3' : '#ffebe9';
              const statusEmoji = isSuccess ? '✅' : '❌';

              const htmlBody = `
                <!DOCTYPE html>
                <html>
                <head>
                    <meta charset="UTF-8">
                    <style>
                        body { 
                          font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; 
                          margin: 0; 
                          padding: 0; 
                          background-color: #f6f8fa; 
                        }
                        .container { 
                          max-width: 600px; 
                          margin: 0 auto; 
                          background: #ffffff; 
                          border: 1px solid #d1d9e0; 
                          border-radius: 6px; 
                        }
                        .header { 
                          background: ${statusColor}; 
                          color: white; 
                          padding: 20px; 
                          text-align: center; 
                        }
                        .header h1 { 
                          margin: 0; 
                          font-size: 20px; 
                          font-weight: 600; 
                        }
                        .content { 
                          padding: 24px; 
                        }
                        .status { 
                          background: ${statusBg}; 
                          color: ${statusColor}; 
                          padding: 8px 12px; 
                          border-radius: 6px; 
                          font-weight: 500; 
                          display: inline-block; 
                          margin-bottom: 16px; 
                        }
                        .info-table { 
                          width: 100%; 
                          border-collapse: collapse; 
                          margin: 16px 0; 
                        }
                        .info-table td { 
                          padding: 8px 0; 
                          border-bottom: 1px solid #eaecf0; 
                        }
                        .info-table td:first-child { 
                          font-weight: 500; 
                          width: 120px; 
                          color: #656d76; 
                        }
                        .info-table td:last-child { 
                          font-family: 'Monaco', 'Menlo', monospace; 
                          font-size: 12px; 
                        }
                        .docker-info { 
                          background: #f6f8fa; 
                          border: 1px solid #d1d9e0; 
                          border-radius: 6px; 
                          padding: 16px; 
                          margin: 16px 0; 
                        }
                        .tag { 
                          background: #0969da; 
                          color: white; 
                          padding: 4px 8px; 
                          border-radius: 12px; 
                          font-size: 12px; 
                          margin: 2px 4px 2px 0; 
                          display: inline-block; 
                          font-family: monospace;
                        }
                        .button { 
                          background: ${isSuccess ? '#1f883d' : '#d1242f'}; 
                          color: white; 
                          text-decoration: none; 
                          padding: 8px 16px; 
                          border-radius: 6px; 
                          font-weight: 500; 
                          margin: 8px 8px 0 0; 
                          display: inline-block; 
                        }
                        .footer { 
                          background: #f6f8fa; 
                          padding: 16px; 
                          text-align: center; 
                          font-size: 12px; 
                          color: #656d76; 
                          border-top: 1px solid #d1d9e0; 
                        }
                    </style>
                </head>
                <body>
                    <div class="container">
                        <div class="header">
                            <h1>${statusEmoji} Weather App Deployment</h1>
                            <p>Docker image ${isSuccess ? 'build completed successfully' : 'build failed'}</p>
                        </div>
                        <div class="content">
                            <div class="status">${statusEmoji} ${statusText}</div>
                            
                            <table class="info-table">
                                <tr>
                                    <td>Repository:</td>
                                    <td>${GITHUB_REPOSITORY}</td>
                                </tr>
                                <tr>
                                    <td>Author:</td>
                                    <td>${GITHUB_ACTOR}</td>
                                </tr>
                                <tr>
                                    <td>Commit:</td>
                                    <td>${GIT_SHA}</td>
                                </tr>
                                <tr>
                                    <td>Run:</td>
                                    <td>${RUN_ID}</td>
                                </tr>
                                <tr>
                                    <td>Build Time:</td>
                                    <td>${new Date().toISOString()}</td>
                                </tr>
                            </table>
                            
                            ${isSuccess ? `
                            <div class="docker-info">
                                <strong>🐳 Docker Tags:</strong><br>
                                <span class="tag">${DOCKER_IMAGE_BACKEND}</span>
                                <span class="tag">${DOCKER_IMAGE_FRONTEND}</span>
                            </div>
                            
                            <a href="https://hub.docker.com/r/jouk2310/weather-backend" class="button">View Backend on Docker Hub</a>
                            <a href="https://hub.docker.com/r/jouk2310/weather-frontend" class="button">View Frontend on Docker Hub</a>
                            ` : ''}
                            
                            <a href="https://github.com/${GITHUB_REPOSITORY}/actions/runs/${RUN_ID}" class="button">View Build Logs</a>
                        </div>
                        <div class="footer">
                            Automated notification from GitHub Actions CI/CD<br>
                            Weather App © 2024
                        </div>
                    </div>
                </body>
                </html>
              `;

              const mailOptions = {
                from: `Weather App CI/CD <${SMTP_USERNAME}>`,
                to: MAIL_TO,
                subject: `${statusEmoji} Weather App - Docker ${statusText}`,
                html: htmlBody
              };

              const info = await transporter.sendMail(mailOptions);
              console.log("✅ Docker build email sent successfully:", info.messageId);

            } catch (error) {
              console.error("❌ Error sending Docker build email:", error);
              process.exit(1);
            }
          })();
          CJSEOF

          node send-docker-email.cjs

  Kubernetes_Email_Notification:
    needs: [Kubernetes_Deploy]
    runs-on: ubuntu-latest
    if: ${{ always() }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install nodemailer
        run: npm install nodemailer --no-audit --no-fund

      - name: Send Kubernetes Deployment Email
        env:
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          MAIL_TO: ${{ secrets.MAIL_TO }}
          DEPLOYMENT_STATUS: ${{ needs.Kubernetes_Deploy.result }}
          GIT_SHA: ${{ github.sha }}
          RUN_ID: ${{ github.run_id }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_ACTOR: ${{ github.actor }}
        run: |
          cat > send-k8s-email.cjs <<'CJSEOF'
          const nodemailer = require("nodemailer");

          (async () => {
            try {
              const SMTP_USERNAME = process.env.SMTP_USERNAME;
              const SMTP_PASSWORD = process.env.SMTP_PASSWORD;
              const MAIL_TO = process.env.MAIL_TO;
              const DEPLOYMENT_STATUS = process.env.DEPLOYMENT_STATUS;
              const GIT_SHA = process.env.GIT_SHA;
              const RUN_ID = process.env.RUN_ID;
              const GITHUB_REPOSITORY = process.env.GITHUB_REPOSITORY;
              const GITHUB_ACTOR = process.env.GITHUB_ACTOR;

              console.log("Deployment Status:", DEPLOYMENT_STATUS);

              const transporter = nodemailer.createTransport({
                service: 'gmail',
                auth: {
                  user: SMTP_USERNAME,
                  pass: SMTP_PASSWORD
                }
              });

              await transporter.verify();
              console.log("✔ SMTP connection verified for K8s notification");

              const isSuccess = DEPLOYMENT_STATUS === 'success';
              const statusText = isSuccess ? 'Deployment Successful' : 'Deployment Failed';
              const statusColor = isSuccess ? '#1f883d' : '#d1242f';
              const statusBg = isSuccess ? '#d1f5d3' : '#ffebe9';
              const statusEmoji = isSuccess ? '🚀' : '❌';

              const htmlBody = `
                <!DOCTYPE html>
                <html>
                <head>
                    <meta charset="UTF-8">
                    <style>
                        body { 
                          font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; 
                          margin: 0; 
                          padding: 0; 
                          background-color: #f6f8fa; 
                        }
                        .container { 
                          max-width: 600px; 
                          margin: 0 auto; 
                          background: #ffffff; 
                          border: 1px solid #d1d9e0; 
                          border-radius: 6px; 
                        }
                        .header { 
                          background: ${statusColor}; 
                          color: white; 
                          padding: 20px; 
                          text-align: center; 
                        }
                        .header h1 { 
                          margin: 0; 
                          font-size: 20px; 
                          font-weight: 600; 
                        }
                        .content { 
                          padding: 24px; 
                        }
                        .status { 
                          background: ${statusBg}; 
                          color: ${statusColor}; 
                          padding: 8px 12px; 
                          border-radius: 6px; 
                          font-weight: 500; 
                          display: inline-block; 
                          margin-bottom: 16px; 
                        }
                        .footer { 
                          background: #f6f8fa; 
                          padding: 16px; 
                          text-align: center; 
                          font-size: 12px; 
                          color: #656d76; 
                          border-top: 1px solid #d1d9e0; 
                        }
                        .button { 
                          background: ${statusColor}; 
                          color: white; 
                          text-decoration: none; 
                          padding: 8px 16px; 
                          border-radius: 6px; 
                          font-weight: 500; 
                          display: inline-block; 
                        }
                    </style>
                </head>
                <body>
                    <div class="container">
                        <div class="header">
                            <h1>${statusEmoji} Weather App Kubernetes</h1>
                            <p>${isSuccess ? 'Successfully deployed to Kubernetes' : 'Deployment to Kubernetes failed'}</p>
                        </div>
                        <div class="content">
                            <div class="status">${statusEmoji} ${statusText}</div>
                            
                            <p><strong>Repository:</strong> ${GITHUB_REPOSITORY}</p>
                            <p><strong>Author:</strong> ${GITHUB_ACTOR}</p>
                            <p><strong>Commit:</strong> ${GIT_SHA}</p>
                            <p><strong>Namespace:</strong> default</p>
                            <p><strong>Deployment Time:</strong> ${new Date().toISOString()}</p>
                            
                            <a href="https://github.com/${GITHUB_REPOSITORY}/actions/runs/${RUN_ID}" class="button">View Deployment Logs</a>
                        </div>
                        <div class="footer">
                            Automated notification from GitHub Actions CI/CD<br>
                            Weather App © 2024
                        </div>
                    </div>
                </body>
                </html>
              `;

              const mailOptions = {
                from: `Weather App CI/CD <${SMTP_USERNAME}>`,
                to: MAIL_TO,
                subject: `${statusEmoji} Weather App - Kubernetes ${statusText}`,
                html: htmlBody
              };

              const info = await transporter.sendMail(mailOptions);
              console.log("✅ Kubernetes deployment email sent successfully:", info.messageId);

            } catch (error) {
              console.error("❌ Error sending Kubernetes deployment email:", error);
              process.exit(1);
            }
          })();
          CJSEOF

          node send-k8s-email.cjs
