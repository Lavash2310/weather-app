kind: Service
apiVersion: v1
metadata:
  name: my-app-service
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4173
  type: NodePort
  ports:
    - port: 80
      targetPort: 4173
      nodePort: 30007